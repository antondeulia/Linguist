export type TrackToReturn = {
	id: string
	name: string
	isCompleted: boolean
	isAvailable: boolean
	units: any[]
}

export type UnitToReturn = {
	id: string
	name: string
	isCompleted: boolean
	isAvailable: boolean
}
